plugins {
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'Diascope'

final File baseDir = file('.')
baseDir.eachDir { dir ->
	final String d = dir.name
	if (d.startsWith('module-') || d.startsWith('lib-')) include d
}
